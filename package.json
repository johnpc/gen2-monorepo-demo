{
  "name": "monorepo-typescript",
  "version": "1.0.0",
  "description": "A monorepo in TypeScript",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build --verbose"
  },
  "dependencies": {
    "aws-amplify": "^5.3.11",
    "typescript": "^5.2.2"
  },
  "devDependencies": {
    "@types/node": "^20.8.7",
    "ts-node": "^10.9.1"
  }
}
